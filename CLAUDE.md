<!--
  Claude Code reads CLAUDE.md, not AGENTS.md, and has no setting to change that.
  This file exists only to import AGENTS.md, which is the authoritative source.
  Add Claude-specific instructions below the import if ever needed; do NOT
  restate rules from AGENTS.md here, as the copies will drift out of sync.
  This comment is stripped before loading, so it costs no context.
-->

@AGENTS.md
